// 导入Vue
import Vue from "vue"
// 导入路由
import VueRouter from 'vue-router'

// 使用路由
Vue.use(VueRouter)

// 编写路由信息
const routes = [

]


// 实例化路由对象
const router = new VueRouter({
    routes
})

// 暴露路由
export default router